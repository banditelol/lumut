# Codex workspace workflows

This project uses the following repository-local workflows:

- Setup: `make install`
- Build the AnyWidget ES module: `make build`
- Run focused tests: `make test`
- Watch the JavaScript widget during notebook inspection: `make dev`

The authoritative implementation guidance for Codex is in the root `AGENTS.md`.

